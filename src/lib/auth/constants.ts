/**
 * localStorage keys used across the auth flow.
 * Centralised here so the login page and AuthProvider always agree on the key names.
 */
export const TOS_KEY = "dyno-phi:tos-accepted";
export const CONTACT_OPT_IN_KEY = "dyno-phi:contact-optin";
